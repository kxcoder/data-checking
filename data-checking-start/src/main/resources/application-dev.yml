spring:
  datasource:
    url: jdbc:mysql://localhost:3306/data_checking_dev?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root123
  kafka:
    bootstrap-servers: localhost:9092
  data:
    redis:
      host: localhost
      port: 6379
      password: ""

dubbo:
  registry:
    address: zookeeper://localhost:2181

logging:
  level:
    com.example.datachecking: DEBUG
